{{#section 'title'}}
    <title>fk - Edit Profile</title>
{{/section}}

{{#section 'style'}}
    <link href="/css/profile.css" rel="stylesheet">
{{/section}}


<div class="card-deck mb-3 mt-5">
    <div class="card mb-4 box-shadow">
        <div class="card-header">
            <h4 class="my-0 font-weight-normal">{{index}}</h4>
        </div>
        <div class="card-body">
            <form action="" method="POST">
                <div class="profile-form-constrainer">
                    {{#each data}}
                        <div class="form-group row mb-0">
                            <label for="{{@key}}" class="col-sm-3 col-form-label">{{this.label}}:</label>
                            <div class="col-sm-8">
                                <input name="{{@key}}"type="text" {{#unless edit}}readonly{{/unless}}{{#if edit}}{{#unless this.editable}}readonly{{/unless}}{{/if}}class="form-control" id="{{@key}}" value="{{this.value}}">
                            </div>
                        </div>
                    {{/each}}
                </div>
                <if

            </form>
        </div>

    </div>
</div>